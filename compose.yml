---
version: '3.8'

services:
  app:
    build: .
    volumes:
      - .:/usr/src/app:cached
      - module-data:/usr/src/app/node_modules:delegated
    environment:
      DEBUG: nightmare*,electron:*

volumes:
  module-data:
    driver: local
